import { reactive } from 'vue'

export default function useSudokuMap () {
  const createMap = (size: number) => {
    // Create DOM table depending on MAP_SIZE
    // ERROR: Dont allow creating maps of sizes other than 9 and 16
  }

  return { createMap }
}